server:
  port: 8087

spring:
  application:
    name: kcet-rank-predictor
  datasource:
    url: jdbc:mysql://localhost:3306/kcet_rank_predictor?createDatabaseIfNotExist=true
    username: root
    password: Anni@2005
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true